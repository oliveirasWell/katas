[project]
name = "generic_python"
version = "0.0.0"
requires-python = ">=3.11"

[dependency-groups]
dev = ["pytest", "pytest-watcher"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
